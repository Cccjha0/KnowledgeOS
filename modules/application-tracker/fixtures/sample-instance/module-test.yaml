contract_version: 1
scenarios:
  normal_capture: { fixture: fixtures/sample-instance/capture-test.yaml }
  ambiguous_capture: { fixture: tests/behavior/normal-ambiguous-error-duplicate.yaml, expected: failed, expected_error_code: APPLICATION_RECORD_AMBIGUOUS }
  permission_denied: { target: 20-Workspace/Experience Log/forbidden.md }
  resource_unavailable: { resource: codex, expected: waiting-for-ai }
  repeat_execution: { enabled: true }
  paused_instance: { enabled: true }
  archived_instance: { enabled: true }
  prompt_regression: { fixture: tests/prompt-regression/facts.yaml }
  periodic_job: { enabled: false }
  event_consumption: { enabled: false }
  event_publication:
    enabled: true
    event_type: research.required
    workflow_id: sync-due-research
    instance_id: null
    seed:
      record_path: 20-Workspace/Applications/sample-applications/Records/Fixture-C6007.md
      next_check: "2026-01-01T00:00:00Z"
  pdf_policy: { enabled: true, partial_expected: review }
  migration_apply: { enabled: true, rollback: true }
